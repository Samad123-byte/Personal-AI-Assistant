# Your n8n webhook URL (this is the only variable you actually need on Vercel).
N8N_WEBHOOK_URL=https://abdul-khan.app.n8n.cloud/webhook/personal-assistant

# Local dev only — Vercel sets PORT itself in production.
PORT=5000
